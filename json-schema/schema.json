{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://stac-extensions.github.io/mlhub-dataset/v1.0.0/schema.json#",
  "title": "MLHub Dataset Extention",
  "description": "Radiant Earth Foundation MLHub Extension for STAC Catalogs.",
  "oneOf": [
    {
      "$comment": "This is the schema for STAC Catalog.",
      "type": "object",
      "allOf": [
        {
          "required": [
            "type"
          ],
          "properties": {
            "type": {
              "const": "Catalog"
            }
          }
        },
        {
          "$ref": "#/definitions/stac_extensions"
        }
      ],
      "anyOf": [
        {
          "$comment": "This is the schema for the top-level fields in a Catalog. Remove this if this extension does not define top-level fields for Catalogs.",
          "allOf": [
            {
              "$ref": "#/definitions/required_fields"
            },
            {
              "$ref": "#/definitions/fields"
            }
          ]
        }
      ]
    }
  ],
  "definitions": {
    "stac_extensions": {
      "type": "object",
      "required": [
        "stac_extensions"
      ],
      "properties": {
        "stac_extensions": {
          "type": "array",
          "contains": {
            "const": "https://stac-extensions.github.io/mlhub-dataset/v1.0.0/schema.json"
          }
        }
      }
    },
    "required_fields": {
      "$comment": "Please list all fields here so that we can force the existance of one of them in other parts of the schemas.",
      "allOf": [
        {"required": ["mlhub:tags"]},
        {"required": ["mlhub:creator_contact"]}
      ]
    },
    "fields": {
      "$comment": "Add your new fields here. Don't require them here, do that above in the corresponding schema.",
      "type": "object",
      "properties": {
        "mlhub:tags": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string"
          }
        },
        "mlhub:creator_contact": {
          "type": "array",
          "uniqueItems": true,
          "minItems": 1,
          "items": {
              "$ref": "#/definitions/creator_contact"
          }
        },
        "mlhub:publications": {
          "type": "array",
          "uniqueItems": true,
          "minItems": 0,
          "items": {
              "$ref": "#/definitions/external_resource"
          }
        },
        "mlhub:tools_apps": {
          "type": "array",
          "uniqueItems": true,
          "minItems": 0,
          "items": {
              "$ref": "#/definitions/external_resource"
          }
        },
        "mlhub:tutorials": {
          "type": "array",
          "uniqueItems": true,
          "minItems": 0,
          "items": {
              "$ref": "#/definitions/external_resource"
          }
        }
      },
      "patternProperties": {
        "^(?!mlhub:)": {}
      },
      "additionalProperties": false
    },
    "creator_contact": {
      "title": "Creator Contact",
      "type": "object",
      "properties": {
        "contact": {
          "title": "Primary email address for POC",
          "type": "string"
        },
        "creator": {
          "title": "Name and URL of affiliations",
          "type": "string"
        }
      }
    },
    "external_resource": {
      "title": "External Resource",
      "type": "object",
      "properties": {
        "url": {
          "title": "URL to external resource",
          "type": "string"
        },
        "title": {
          "title": "Title of linked resource",
          "type": "string"
        },
        "author_url": {
          "title": "Webpage or social media account",
          "type": "string"
        },
        "author_name": {
          "title": "Name of author(s) of external resource",
          "type": "string"
        }
      }
    }
  }
}
